[Unit]
Description=Timer: analyst hourly (*-*-* *:05:00 UTC)

[Timer]
OnCalendar=*-*-* *:05:00
# Run a missed slot at the next boot instead of skipping it.
Persistent=true
Unit=multinode_aq_analyst_hourly.service

[Install]
WantedBy=timers.target
